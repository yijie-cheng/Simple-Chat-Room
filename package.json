{
  "name": "simple-chat-room",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && yarn && cd ../backend && yarn",
    "start": "cd frontend && yarn start",
    "server": "cd backend && yarn server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongoose": "^8.3.2"
  }
}
